services:
   web:
       build: .
       command: bash -c "python src/preprocess.py && python src/train.py && pytest src/tests/ --cov=src --cov-report=html && uvicorn src.api.app:app --host 0.0.0.0 --port 8000 --reload"
       ports:
           - 8000:8000
       image: zwttt/ml_pipeline:latest